<!-- Generated by LVGL Figma Flow | This file is OWNED_BY=FIGMA -->
<!-- Change to OWNED_BY=USER to take ownership and prevent regeneration -->
<component>
	<styles>
		<!-- Survival only. After remove_style_all the widget is bare so Figma's
         design drives every visible attr; we add back the minimum that would
         otherwise DISAPPEAR. The closed button keeps a readable text color
         (the emitter overrides it from the drawn label). -->
		<style name="style_main" text_color="0x000000" />
		<!-- The open list popup is NEVER drawn in Figma, so the design can't style
         it. Give it functional survival styling: an opaque, readable surface
         and a visible highlight for the selected / pressed option. -->
		<style
			name="style_list"
			bg_color="0xFFFFFF"
			bg_opa="100%"
			text_color="0x000000"
			border_color="0xf00"
			border_width="1"
			radius="4"
			pad_all="4"
		/>

		<style name="style_chevron" bg_image_src="flower_data" />

		<style name="style_list_selected" bg_color="0xf00" text_color="0x000" />
	</styles>
	<view extends="lv_dropdown">
		<remove_style_all />
		<style name="style_main" />
		<style name="style_chevron" selector="indicator" />
		<lv_dropdown-list>
			<style name="style_list" />
			<style name="style_list_selected" selector="selected|checked" />
		</lv_dropdown-list>
	</view>
</component>
